{
  "name": "@portalo/mcp-server",
  "version": "0.2.0",
  "description": "MCP server for managing Portalo link-in-bio pages via AI",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "bin": {
    "portalo-mcp": "./dist/index.js"
  },
  "scripts": {
    "build": "tsup src/index.ts --format cjs --dts",
    "dev": "tsup src/index.ts --format cjs --dts --watch",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.11.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsup": "^8.0.0",
    "typescript": "^5.7.0"
  },
  "files": [
    "dist"
  ],
  "keywords": [
    "mcp",
    "portalo",
    "link-in-bio"
  ],
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/ctejada/portalo",
    "directory": "packages/mcp-server"
  },
  "engines": {
    "node": ">=18"
  }
}
